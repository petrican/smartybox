import styles from './LoginForm.module.css';
import {Component} from "solid-js";

const LoginForm: Component = () => {
  return (
    <div class={styles.loginForm}>Login form</div>
  );
};

export default LoginForm;
